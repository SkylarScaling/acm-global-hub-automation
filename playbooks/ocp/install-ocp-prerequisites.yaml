---
- name: Install Pre-Requisites for OCP Playbooks
  gather_facts: false
  connection: local
  hosts: localhost
  vars_files:
    - ../group_vars/all.yaml
  roles:
    - ocp/install_ocp_prerequisites
...
